#include <iostream>
#include <climits>
using namespace std;

#define pb push_back
#define sz(x) (int)(x.size())
typedef long long LL;

int memo[5005][5005];

void dp(int s

int main(){
  int t = 1;
  while(t--){
    getline(cin, S);
    getline(cin, T);
  }
}