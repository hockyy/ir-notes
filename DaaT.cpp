#include <bits/stdc++.h>
using namespace std;

void next(double tetha, vector <PII> &postingIndex){
  while(1) {
    // Sort the documents by document ID;
    sort(begin(postingIndex), end(postingIndex), )
  }
}

int main() {
  int n; cin >> n;
  cout << n << endl;
}